{
  "name": "arbitrage-engine",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Source-local / flip-on-eBay arbitrage engine. Connectors -> identify -> comp -> match -> score.",
  "scripts": {
    "dev": "tsx watch src/run.ts",
    "run": "tsx src/run.ts",
    "fb:login": "tsx src/fb-login.ts",
    "fb:import": "tsx src/fb-import.ts",
    "dashboard": "tsx src/server.ts",
    "serve": "WATCH_IN_SERVER=true tsx src/server.ts",
    "watch": "tsx src/watch.ts",
    "doctor": "tsx src/doctor.ts",
    "doctor:actors": "tsx src/doctor-actors.ts",
    "debug": "tsx src/debug.ts",
    "clean:browsers": "npx playwright uninstall --all && npx playwright install chromium",
    "test": "node --import tsx --test test/*.test.ts",
    "build": "tsc -p tsconfig.json",
    "start": "node dist/run.js",
    "prisma:generate": "prisma generate",
    "prisma:push": "prisma db push"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.32.1",
    "fast-xml-parser": "^4.5.0",
    "tsx": "^4.19.2",
    "undici": "^6.21.0"
  },
  "devDependencies": {
    "@types/node": "^22.9.0",
    "typescript": "^5.6.3"
  },
  "optionalDependencies": {
    "@prisma/client": "^5.22.0",
    "playwright": "^1.61.0",
    "prisma": "^5.22.0"
  }
}
